# Aside Host bundle patch: one additive row over the shipped Web composition.
# A package declaring `dsh.bundle.patch` joins the profile layer stack when
# installed with `dsh plugin add`, so this row composes automatically.
- insert:
    - id: aside-host
      name: 'dsh-aside-host'
